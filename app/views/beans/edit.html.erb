<div class="flex flex-col items-center w-[700px] mx-auto my-[100px]">
  <div class="w-full text-black">
    <div class="text-center font-bold">
      <h2 class="text-4xl font-heading"><%= t('.title') %></h2>
    </div>

    <!-- 投稿フォームのパーシャルを表示 -->
    <%= render 'form', bean: @bean %>

    <!-- 豆詳細へ戻るボタン -->
    <div class="text-center font-body w-full mt-[50px]">
      <%= link_to t('.back_to_show_bean'), bean_path(@bean), class: "btn btn-accent w-[200px] h-[50px]" %>
    </div>
  </div>
</div>
