<!-- 投稿のブックマークの状態に応じてボタンを出し分けるパーシャル -->
<% if current_user.bean_bookmarked?(bean) %>
  <!-- ブックマーク済みの場合は、ブックマーク解除ボタンを表示 -->
  <%= render 'beans/unbookmark', bean: bean %>
<% else %>
  <!-- ブックマークしていない場合は、ブックマークボタンを表示 -->
  <%= render 'beans/bookmark', bean: bean %>
<% end %>
